#version 460
layout(push_constant) uniform pushBlock {
    int total;
    int batchsize;
    float eps;
    float momentum;
} p;


layout (local_size_x = 64, local_size_y = 64, local_size_z = 1) in;

layout (binding = 0) readonly buffer ssbA {
    float A[];
};

layout (binding = 1)  buffer ssbC {
  float C[];
};


void main(){

}